# Local Terraform

This stack is informational only. It exposes values from `config/Infrastructure/local.yaml` for tooling consistency.

## Usage

```bash
cd config/Infrastructure/terraform/local
terraform init
terraform apply
```
